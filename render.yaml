services:
  - type: web
    name: wa-checker
    env: node
    plan: starter
    buildCommand: bash render-build.sh
    startCommand: node app.js
    envVars:
      - key: NODE_ENV
        value: production
